# Reachy Mini GGUF Assistant
# Tested on: Jetson Orin Nano 8GB, JetPack 6, Python 3.10
#
# Modified by the reachy-mini-gguf-assistant contributors, 2026:
# faster-whisper and kokoro-onnx moved out of the default install - the two
# GGUF models on llama.cpp replace them. ChromaDB likewise: RAG is off.

# Core
pyyaml>=6.0
rich>=13.0
typer>=0.9.0
psutil>=5.9.0

# Audio I/O
sounddevice>=0.4.6

# VAD — Silero neural voice activity detection (much better than energy-only)
silero-vad>=5.1

# LLM and TTS clients — both servers speak HTTP
httpx>=0.24.0

# Face tracking — YuNet face detection via OpenCV (cv2.FaceDetectorYN)
# No extra pip dependencies; uses opencv already listed

# Vision — camera capture
opencv-python-headless>=4.8.0

# Web UI (optional — only needed for run_web_vision_chat.py)
fastapi>=0.109.1  # excludes Content-Type header ReDoS-affected releases
uvicorn>=0.24.0

# Optional — only for pipeline.mode "split", or transcribe_for_display:
#   pip install faster-whisper
# Optional — only for rag.enabled (needs an embedding server of your own):
#   pip install chromadb

# Reachy Mini SDK (robot control)
# Installed separately: pip install reachy-mini

# Jetson-specific (install separately, see README):
#   pip install onnxruntime-gpu --extra-index-url https://pypi.jetson-ai-lab.io/jp6/cu126
#   pip install numpy==1.26.4
